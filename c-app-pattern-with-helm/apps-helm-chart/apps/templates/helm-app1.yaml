{{- $top := . -}}
#{{- range $application :=  .Values.applications -}}

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: helm-app1
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: helm-app1
    server: {{ $top.server }}
  project: default
  source:
    path: helm-guestbook
    repoURL: {{ $top.repoURL }}
    targetRevision: {{ $top.targetRevision }}
  syncPolicy:
    automated:
      allowEmpty: true
      selfHeal: true
    syncOptions:
    - allowEmpty=true
    - CreateNamespace=true
#{{ end }}
